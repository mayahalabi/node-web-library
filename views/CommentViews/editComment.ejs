<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Comment - Edit</title>

    <!-- Link to Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous" />

    <style>
        body {
            background-color: #f6f4f2;
            /* Light beige background */
            font-family: "Arial", sans-serif;
        }

        .container {
            background-color: #ffffff;
            /* White background for the form container */
            border-radius: 8px;
            /* Rounded corners */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            /* Soft shadow around the container */
            padding: 40px;
            max-width: 600px;
            margin-top: 50px;
        }

        h1 {
            color: #425664;
            /* Dark grey-blue for the title */
            font-size: 30px;
            text-align: center;
            margin-bottom: 20px;
        }

        .card {
            border: none;
            background-color: #d9d9d9;
            /* Light grey card background */
            border-radius: 8px;
            margin-bottom: 30px;
            padding: 20px;
        }

        .card h4 {
            color: #425664;
            /* Dark grey-blue for the subtitle */
            font-size: 22px;
            margin-bottom: 10px;
        }

        .card ul {
            color: #425664;
            font-size: 18px;
        }

        .form-group label {
            color: #425664;
            font-weight: bold;
        }

        .form-control {
            border-radius: 6px;
            border: 1px solid #c6ad8f;
            /* Beige-colored border */
            padding: 12px;
            font-size: 16px;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            border-color: #425664;
            /* Focus color for the input fields */
            box-shadow: 0 0 5px rgba(66, 86, 100, 0.5);
            /* Soft shadow when focused */
        }

        .btn-primary {
            background-color: #c6ad8f;
            /* Beige-colored button */
            color: #ffffff;
            /* White text */
            font-size: 16px;
            padding: 12px;
            border-radius: 6px;
            border: none;
            width: 100%;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            background-color: #425664;
            /* Dark grey-blue when hovered */
            color: #ffffff;
            /* White text when hovered */
        }

        .btn-primary:active {
            background-color: #d9d9d9;
            /* Light grey when clicked */
            color: #425664;
        }

        .btn-back {
            background-color: #6c757d;
            /* Neutral gray color for the Back button */
            color: white;
            border: none;
            font-size: 16px;
            padding: 12px;
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-bottom: 20px;
            /* Space below the button */
        }

        .btn-back:hover {
            background-color: #5a6268;
            /* Slightly darker gray for hover effect */
            color: white;
        }

        .btn-back:active {
            background-color: #4a545a;
            /* Dark gray when clicked */
            color: white;
        }

        /* Reduced space between form fields */
        .form-group {
            margin-bottom: 10px;
            /* Adjusted space between fields */
        }

        .footer {
            text-align: center;
            margin-top: 30px;
            color: #425664;
            font-size: 14px;
        }
    </style>
</head>

<body>
    <div class="container">
        <!-- Back Button at Top Left -->
        <a href="/api/comments" class="btn btn-back">
            <i class="bi bi-arrow-left-circle"></i> Back
        </a>

        <h1>Edit Comment with ID <%= comment.comment_id %>
        </h1>

        <!-- Display Comment Information in a Card -->
        <div class="card">
            <h4>Comment Information</h4>
            <ul>
                <li><b>ID:</b>
                    <%= comment.comment_id %>
                </li>
                <li><b>Book ID:</b>
                    <%= comment.book_id %>
                </li>
                <li><b>Username:</b>
                    <%= comment.username %>
                </li>
                <li><b>Rating:</b>
                    <%= comment.rating %>
                </li>
                <li><b>Comment Date:</b>
                    <%= comment.comment_date %>
                </li>
                <li><b>Comment Description:</b>
                    <%= comment.comment_description %>
                </li>
            </ul>
        </div>

        <!-- Edit Comment Form -->
        <form action="/api/comments/update-comment/<%= comment.comment_id %>" method="POST">

            <!-- Textarea for comment description -->
            <div class="form-group">
                <label for="comment_description">Description</label>
                <textarea id="comment_description" name="comment_description" class="form-control"
                    placeholder="Enter description" required><%= comment.comment_description %></textarea>
            </div>


            <!-- Dropdown for rating -->
            <div class="form-group">
                <label for="rating">Rating (1-5):</label>
                <select name="rating" id="rating" class="form-control" required>
                    <option value="1">1 - Poor</option>
                    <option value="2">2 - Fair</option>
                    <option value="3">3 - Good</option>
                    <option value="4">4 - Very Good</option>
                    <option value="5">5 - Excellent</option>
                </select>
            </div>

            <button type="submit" class="btn btn-primary">Save Changes</button>
        </form>
    </div>

    <!-- Include Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-pzjw8f+ua7Kw1TIq0Xn7RIF3kXov2+K7w0a+Y3hT6uY8URxx+VdCzXj7mVt8V+Xb"
        crossorigin="anonymous"></script>
</body>

</html>