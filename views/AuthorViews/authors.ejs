<!DOCTYPE html>
<html>

<head>
  <link href="/css/style.css" rel="stylesheet" type="text/css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css"
    integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet" />
  <title>Authors</title>
  <style>
    /* Custom styles for buttons */
    .btn-update {
      background-color: #425664;
      color: white;
      border: none;
    }

    .btn-update:hover {
      background-color: #2f3d48;
      color: #ffffff;
    }

    .btn-delete {
      background-color: #c6ad8f;
      color: white;
      border: none;
    }

    .btn-delete:hover {
      background-color: #b09579;
      color: #ffffff;
    }

    .btn-add {
      background-color: #6c757d;
      /* Neutral gray color for the Add button */
      color: white;
      border: none;
      font-size: 1rem;
      padding: 10px 15px;
      /* Added padding for a better button size */
      margin-bottom: 20px;
      /* Space below the button */
    }

    .btn-add:hover {
      background-color: #5a6268;
      /* Slightly darker gray for hover effect */
      color: white;
    }

    .btn {
      font-size: 0.875rem;
      /* Small button font size */
    }

    /* Header Styling */
    h2 {
      color: #425664;
      /* You can change this to your preferred color */
      margin-bottom: 20px;
      /* Space between the header and the table */
    }

    /* Table Styling */
    .table th,
    .table td {
      vertical-align: middle;
      /* Align text vertically in the middle */
    }

    .table td {
      padding: 0.75rem;
      /* Adjust padding to make the table more compact */
    }

    .container {
      margin-top: 20px;
    }

    /* Ensure the Add Author button stays at the bottom-right of the table */
    .add-author-container {
      text-align: right;
      margin-bottom: 15px;
    }
  </style>
</head>

<body>
  <%- include('../adminPartial/navbarAdmin', {role: 'admin' }) %>

    <div class="container">
      <!-- Add Author Button Positioned at Bottom Right -->
      <div class="add-author-container">
        <a href="/api/authors/add-form" class="btn btn-add">
          <i class="bi bi-plus-circle"></i> Add Author
        </a>
      </div>

      <!-- Header for Author Management -->
      <h2><b>Author Management</b></h2>

      <!-- Table displaying authors -->
      <table class="table table-striped">
        <thead>
          <tr>
            <th>Author Id</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% for (let i=0; i < authors.length; i++) { %>
            <tr>
              <td>
                <%= authors[i].author_id %>
              </td>
              <td>
                <%= authors[i].first_name %>
              </td>
              <td>
                <%= authors[i].last_name %>
              </td>
              <td>
                <!-- Update Button -->
                <a href="/api/authors/edit-form/<%= authors[i].author_id %>" class="btn btn-sm btn-update mx-1"
                  title="Edit Author">
                  <i class="bi bi-pencil"></i> Edit
                </a>

                <!-- Delete Button -->
                <a href="/api/authors/delete/<%= authors[i].author_id %>" title="Delete Author"
                  class="btn btn-sm btn-delete mx-1">
                  <i class="bi bi-trash"></i> Delete
                </a>
              </td>
            </tr>
            <% } %>
        </tbody>
      </table>
    </div>
</body>

</html>