<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <!-- Library Title (Non-clickable) -->
    <span class="navbar-brand" style="font-size: 1.5rem; font-weight: bold">BookWave</span>

    <!-- Toggle Button for Mobile (for all users) -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <% if (role==='user' ) { %>
      <!-- Navbar Links (for users) -->
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <!-- Log Out Button -->
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/" style="font-size: 1.2rem">Log Out</a>
          </li>
        </ul>
      </div>
      <% } else { %>
        <!-- Navbar Links (for other roles) -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <!-- Home Button -->
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/api/users/index" style="font-size: 1.2rem">Home</a>
            </li>
          </ul>
        </div>
        <% } %>

          <!-- Conditional Rendering for User Role -->
          <% if (role==='user' ) { %>
            <div class="d-flex">
              <a class="nav-link" href="/api/notifications/byUser/<%= username %>"
                style="color: white;">Notifications</a>
              <a class="nav-link" href="/api/transactions/byUsernameTransaction/<%= username %>"
                style="color: white;">Borrowed Books</a>
            </div>
            <% } %>

              <!-- Search bar -->
              <% if (role==='user' ) { %>
                <div class="d-flex ms-auto">
                  <!-- Submit button using form -->
                  <form method="GET" action="/api/books/searchView" class="d-inline">
                    <!-- Search input field -->
                    <input name="searchItem" class="form-control me-2" id="searchItem" type="search"
                      placeholder="Search" aria-label="Search" />

                    <!-- Search button -->
                    <button type="submit" class="btn btn-outline-light">Search</button>
                  </form>
                </div>
                <% } %>


  </div>
</nav>